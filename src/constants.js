export const pageSize = 6;
// export const totalPages = products.length;
// export const noOfPages = Math.ceil(totalPages / pageSize);

// export const startVal = currPage * pageSize;
// export const endVal = startVal + pageSize;
